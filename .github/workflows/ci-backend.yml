name: Backend CI

on:
  push:
    branches: [ develop, main ]
    paths:
      - 'docs/backend/**'
      - '.github/workflows/ci-backend.yml'
  pull_request:
    branches: [ develop, main ]
    paths:
      - 'docs/backend/**'

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    # Backend build steps will be added later
